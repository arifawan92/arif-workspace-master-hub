{
  "name": "peos-api",
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": "24.x" },
  "scripts": {
    "build": "echo 'No build needed for API'",
    "dev": "node api/cron/nightly-report.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}